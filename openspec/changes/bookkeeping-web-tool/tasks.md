## 1. 项目脚手架

- [x] 1.1 建立仓库结构 `apps/web`、`apps/api`、`infra`，配置 TypeScript、lint、格式化
- [x] 1.2 初始化 `apps/api`（Node + Fastify），分层 routes→service→repo 骨架
- [x] 1.3 初始化 `apps/web`（React + Vite + TS），移动优先响应式基础布局
- [x] 1.4 配置本地开发：前端代理到 API、环境变量、启动脚本

## 2. 数据库与数据模型

- [x] 2.1 配置 Postgres + Prisma，建立连接与迁移流程
- [x] 2.2 定义 `cards` 表（名称、初始余额、是否默认、排序）
- [x] 2.3 定义 `budget_snapshots` 表（日期，唯一约束）与 `budget_lines` 表（snapshot_id、card_id、in、out）
- [x] 2.4 定义 `transactions` 表（card_id、date、type∈in|out|transfer|adjust、amount、category、note、peer_card_id）
- [x] 2.5 生成首个迁移并校验单表交易模型可表达四类记录

## 3. 鉴权与后端同步

- [x] 3.1 实现单人密码登录换取访问令牌（JWT），令牌过期策略
- [x] 3.2 给所有数据接口加鉴权中间件，未授权返回 401
- [x] 3.3 前端登录页与令牌存储、请求拦截器

## 4. 卡片管理（card-management）

- [x] 4.1 API：卡片增删改查、设默认卡（保证唯一默认）、排序
- [x] 4.2 服务层：删除卡时校验关联流水/默认状态并阻止或提示
- [x] 4.3 前端：卡片管理界面（新增/重命名/删除/设默认/拖拽排序、初始余额）

## 5. 余额派生引擎（budget-planning + daily-bookkeeping 基础）

- [x] 5.1 实现派生余额计算：初始余额 + Σ收 − Σ支 ± 调账净额（按卡、按截止点）
- [x] 5.2 预算层余额只读派生，拒绝直接写余额
- [x] 5.3 实际层手改余额 → 生成 (目标−派生) 的 adjust 流水
- [x] 5.4 单元测试覆盖收/支/调账/调整对余额的影响

## 6. 预收预算（budget-planning）

- [x] 6.1 API：创建/更新某日期预算快照（同日期唯一）、按卡录入收/支/调账
- [x] 6.2 API：返回快照各卡派生余额与合计(balance 行)
- [x] 6.3 前端：预算快照编辑界面（按卡收/支/调账，余额自动显示、合计行）

## 7. 调账（account-transfer）

- [x] 7.1 API：调出/调入两卡同变、原子事务、必须指定对手卡
- [x] 7.2 预算层与实际层共用调账语义
- [x] 7.3 统计聚合一律排除 type=transfer
- [x] 7.4 前端：调账选项 + 调出/调入 + 对手卡选择 UI

## 8. 当日记账（daily-bookkeeping）

- [x] 8.1 API：记一笔 in/out（含分类、金额、日期、备注）
- [x] 8.2 分类清单可配置（收入/支出起步清单，可扩展）
- [x] 8.3 前端：选卡（默认卡选中）→ 收入/支出 → 分类 → 提交
- [x] 8.4 前端：当日记账列表与编辑/删除

## 9. 预算 vs 实际对比（budget-actual-comparison）

- [x] 9.1 实现覆盖周期映射：取日期 ≥ D 的最近快照；超过全部则取最后一个并标记"未填"
- [x] 9.2 API：返回对比基准快照各卡预算余额 + 实际余额 + 差额
- [x] 9.3 前端：顶部各卡对比卡片，实际余额 < 预算余额时整张标红并显示超支额
- [x] 9.4 处理"该期预算未填"提示

## 10. 总结（summary-reporting）

- [x] 10.1 API：预算 vs 实际按卡汇总，标识超支
- [x] 10.2 API：真实收支统计（排除 transfer 与 adjust）
- [x] 10.3 API：年同比(y2y)/月环比(m2m)指标
- [x] 10.4 前端：总结视图（汇总表 + 同比/环比）

## 11. 收尾

- [x] 11.1 端到端联调：预算→记账→对比→总结全链路验证
- [x] 11.2 移动端自适应回归（小屏布局、触控）
- [x] 11.3 最简部署说明（Node + Postgres 单实例、HTTPS、环境变量）
